const page = () => {
  return (
    <div
      className="flex items-center justify-center w-full"
      style={{ height: "calc(100vh - 60px)" }}
    >
      page
    </div>
  );
};

export default page;
